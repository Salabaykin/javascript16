// Создаем переменные
let money, income, addExpenses, deposit, mission, period = 25;

// Выводим всплывающее окно с сообщением
alert('Lesson 1');

// Выводим в консоль браузера сообщение
console.log(`Message - ${period}`);